# factory: creates components
# the component created is identified via the UUID (unique 128-bits number)

# the purpose is to hide the implementation details of how the Component is
# instanciated: the base class of the Component impleme is hidden by the factory

# this is a method of simulator.py
# def register_factory(self, component_factory):
#     pass
